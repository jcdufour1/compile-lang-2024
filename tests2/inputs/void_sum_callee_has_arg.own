type io import = std.io

type Token sum {
    num i32
    open_par
}

fn main() i32 {
    let token Token = .open_par(8)
    switch token {
        case .num(num): break
        case .open_par: break
    }
    return 0
}
