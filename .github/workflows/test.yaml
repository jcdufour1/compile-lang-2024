name: Github Actions Test
run-name: Run Name
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt update 
      - run: sudo apt install clang python3
      - run: python3 do_test.py --test
