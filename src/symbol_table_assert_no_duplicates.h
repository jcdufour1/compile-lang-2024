#ifndef SYMBOL_TABLE_ASSERT_NO_DUPLICATES
#define SYMBOL_TABLE_ASSERT_NO_DUPLICATES

#include <util.h>
#include <symbol_table.h>

//void symbol_table_assert_no_duplicates(const Env* env) {
//    if (env->ancesters.info.count < 1) {
//        return;
//    }
//
//    for (size_t idx_env = 1; idx_env < env->ancesters.info.count; idx_env++) {
//        Symbol_table curr = vec_at(&env->ancesters, idx_env)->symbol_table;
//        for (size_t idx_tbl = 0; idx_tbl < curr.capacity; idx_tbl++) {
//            if (
//            Env temp_env = *env;
//            temp_env->ancesters.info.count = idx_env;
//            try
//        }
//    }
//}

#endif // SYMBOL_TABLE_ASSERT_NO_DUPLICATES

