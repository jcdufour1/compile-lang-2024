#ifndef NODE_IF_PTR_VEC_H
#define NODE_IF_PTR_VEC_H

#include <stddef.h>
#include "vector.h"
#include <assert.h>

struct Node_if_;

typedef struct Node_if_ Node_if;

typedef struct {
    Vec_base info;
    Node_if** buf;
} Node_if_ptr_vec;

#endif // NODE_IF_PTR_VEC_H
