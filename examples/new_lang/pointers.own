
extern("c") fn printf(format_string: u8*, args: any...) i32

fn add_one(num: i32*) {
    deref(num) = num + 1
}

fn main() i32 {
    let num: i32 = 8
    printf("%d\n", num)
    add_one(refer(num))

    return 0
}

