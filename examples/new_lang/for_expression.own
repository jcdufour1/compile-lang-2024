extern("c") fn printf(str_to_print: u8*, args: any...) i32

fn main() i32 {
    let should_loop: i32 = 1
    for should_loop {
        printf("yes\n")
        should_loop = 0
    }
    printf("done\n")
    return 0
}
