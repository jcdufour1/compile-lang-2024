
extern("c") fn printf(str_to_print: u8*, args: any...) i32

struct Div {
    numerator: i32
    denominator: i32
}

fn main() i32 {
    let div: Div = {
        .numerator = 87,
        .denominator = 13
    }

    printf("%d/%d\n", div.numerator, div.denominator)

    return 0
}
