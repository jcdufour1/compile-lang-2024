
Local_Darr :: struct(<ItemType, IndexType>) {
    items ItemType*
    count IndexType
    capacity IndexType
}

Token :: struct(<NumType, ThingType>) {
    num NumType
    thing ThingType
}

darr_do_things :: fn(darr Local_Darr(<Token(<i64, u16>), i32>)) {
}

main :: fn() i32 {
    let darr Local_Darr(<Token(<i64, u8*>), i32>)
    darr_do_things(darr)


    return 0
}
