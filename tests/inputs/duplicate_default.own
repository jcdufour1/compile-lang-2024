
printf :: extern("c") fn(str_to_print u8*, args opaque...) i32

TokenType :: enum {
    num
    string
}

main :: fn() i32 {
    token_type TokenType := TokenType.num
    switch token_type {
        default: printf("unhandled token_type")
        default: printf("unhandled token_type")
    }
    return 0
}
