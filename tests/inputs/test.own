io :: import = std.io


fn get_string() u8* {
    let string u8* = "new programming language"
    return string
}

fn println(string u8*, num2 i32) i32 {
    let string_thing u8* = "67"
    io.puts(string_thing)
    let num1 i32 = 1
    num1 = 3
    num1 = 5
    string_thing = get_string()
    io.puts(string)
    io.puts(string_thing)
    return num2*5
}

fn main() i32 {
    let count_3 i32 = 89
    for let idx_2: i32 in 0..2 {
        io.puts("thing with colon")
    }
    for let idx_2 i32 in 0..2 {
        io.puts("thing without colon")
    }
    for idx_2: i32 in 0..2 {
        io.puts("thing with colon no let")
    }
    for idx_2 i32 in 0..2 {
        io.puts("thing without colon no let")
    }
    let string_thing u8* = get_string()
    let string_thing_2 u8*
    string_thing_2 = "hello"
    io.printf("%s\n", string_thing)
    io.printf("%s: %d\n", string_thing_2, count_3)
    let result i32 = println("bye", 91)
    io.printf("%d\n", result)

    for idx_2 i32 in 0..10 {
        io.printf("idx_2: %d\n", idx_2)
        if idx_2 > 5 {
            break
        }
    }

    return 0
}
