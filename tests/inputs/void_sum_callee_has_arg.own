io :: import = std.io

Token :: enum {
    num i32
    open_par
}

main :: fn() i32 {
    token Token := .open_par(8)
    switch token {
        case .num(num): break
        case .open_par: break
    }
    return 0
}
