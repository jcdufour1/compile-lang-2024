io :: import = std.io

Union :: raw_union {
    num i32
    cstr u8*
}

fun1 :: fn(union Union) {
    io.printf("%d\n", union.num)
}

main :: fn() i32 {
    let union Union

    union.num = 45
    fun1(union)

    union.cstr = "hello\n"
    io.printf("%s", union.cstr)

    return 0
}
