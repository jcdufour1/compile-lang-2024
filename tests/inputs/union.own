io :: import = std.io

type Union raw_union {
    num i32
    cstr u8*
}

fn fun1(union Union) {
    io.printf("%d\n", union.num)
}

fn main() i32 {
    let union Union

    union.num = 45
    fun1(union)

    union.cstr = "hello\n"
    io.printf("%s", union.cstr)

    return 0
}
