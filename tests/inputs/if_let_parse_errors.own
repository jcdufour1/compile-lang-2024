
util :: import = std.util

do_thing :: fn() i32? {
    return .none
}

main :: fn() i32 {
    opt io.Optional(<i32>) := .some(7)
    if .some(num) := opt {
    } else if 1 {
    }

    if num := do_thing() {
    }

    return 0
}
