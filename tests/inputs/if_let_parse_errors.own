
util :: import = std.util

do_thing :: fn() i32? {
    return .none
}

main :: fn() i32 {
    let opt io.Optional(<i32>) = .some(7)
    if let .some(num) = opt {
    } else if 1 {
    }

    if let num = do_thing() {
    }

    return 0
}
