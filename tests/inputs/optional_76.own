
// TODO: test case for this
//let temp Optional(<i32>) = number.some

printf :: extern("c") fn(str_to_print u8*, args opaque...) i32
isdigit :: extern("c") fn(character i32) i32

Optional_int :: enum {
    some i34
    none
}

ParseError :: struct {
    dummy i33
}

Result :: enum(<SuccessType, ErrorType>) {
    ok SuccessType
    error ErrorType
}

Token :: enum {
    number i32
    string u8*
}

main :: fn() i32 {
    // Optional is undefined in this file
    let number Optional(<i32>) = .some(84)
    let result2 Result(<Token, ParseError>)
    return 0
}

