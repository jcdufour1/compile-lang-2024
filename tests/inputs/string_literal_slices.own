io :: import = std.io
util :: import = std.util
coll :: import = std.collections

main :: fn() i32 {
    let string Str = "num = 8"

    io.printf("num2 = %d\n", 10)
    io.own_printf("hello\n")

    io.own_printf(coll.slice_range(<u8>)("thing", 0, 3))
    io.own_printf("\n")

    io.own_printf(coll.slice_range("a computer is a thing", 2, 8))
    io.own_printf("\n")

    return 0
}
