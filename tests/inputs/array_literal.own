io :: import = std.io
coll :: import = std.collections

fn main() i32 {
    let slice coll.Slice(<i32>) = [87, 131]

    io.printf("%d\n", coll.slice_at(<i32>)(slice, 0))
    io.printf("%d\n", coll.slice_at(<i32>)(slice, 1))

    return 0
}
