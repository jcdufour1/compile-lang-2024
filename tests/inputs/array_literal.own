type io import = std.io

fn main() i32 {
    let slice io.Slice(<i32>) = [87, 131]

    io.printf("%d\n", io.slice_at(slice, 0, i32))
    io.printf("%d\n", io.slice_at(slice, 1, i32))

    return 0
}
