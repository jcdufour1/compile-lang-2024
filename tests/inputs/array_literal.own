type io import = std.io

fn main() i32 {
    let slice io.Slice(<i32>) = [87, 131]

    io.printf("%d\n", io.slice_at(<i32>)(slice, 0))
    io.printf("%d\n", io.slice_at(<i32>)(slice, 1))

    return 0
}
