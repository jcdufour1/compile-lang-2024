
extern("c") printf :: fn(format_string u8*, args opaque...) i32

add_one :: fn(num i32*) {
}

add_one_not_ptr :: fn(num i32) {
}

main :: fn() i32 {
    let num i32 = 4

    // should be error
    add_one(num)

    // should be error
    add_one_not_ptr(&num)

    return 0
}
