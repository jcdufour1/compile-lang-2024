
io :: import = std.io
coll :: import = std.collections

Token :: struct {
    num i32
    str coll.Str
}

Node :: enum {
    num i32
    str coll.Str
}

main :: 
fn() i32 {
    io.print_str(
        "hello"
    )

    token Token := {
        .num = 8,
        .str = "bye"
    }
    io.print_str(
        token.
            str
    )


    node Node := .str(
        "bye"
    )
    if .str(
        string
    ) := node {
        io.print_str(
            string
        )
    }
    if 
    .str(
        string
    ) := node {
        io.print_str(
            string
        )
    }

    if .some(file) := io.open_file("tests/inputs/newline.own", "r") {
        io.print_str("yes")
    } 
    else {
        io.print_str("no")
    }

    if .some(file) := io.open_file("tests/inputs/newline.own", "r") {
        io.print_str("yes")
    } 

    else {
        io.print_str("no")
    }

    if .some(file) := io.open_file("tests/inputs/newline.own", "r") {
        io.print_str("yes")
    } 

    else => io.print_str("no")

    return 0
}
