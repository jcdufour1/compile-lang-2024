compile::stdout 0

compile::stderr 10
tests/inputs/if_let_parse_errors.own:11:12:[1;31merror[0;39m:`else if` causes of `if let` statements are not supported
    10 |     if let .some(num) = opt {
    11 |     } else if 1 {
       |            ^
    12 |     }
tests/inputs/if_let_parse_errors.own:14:12:[1;31merror[0;39m:expected enum case (e.g. `.some(num)`) here
    13 | 
    14 |     if let num = do_thing() {
       |            ^
    15 |     }

compile::return_code 1

