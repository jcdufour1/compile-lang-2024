
type Darr struct(<ItemType, IndexType>) {
    items ItemType*
    count IndexType
    capacity IndexType
}

type Token struct(<NumType, ThingType>) {
    num NumType
    thing ThingType
}

fn darr_do_things(darr Darr(<Token(<i64, u16>), i32>)) {
}

fn main() i32 {
    let darr Darr(<Token(<i64, u8*>), i32>)
    darr_do_things(darr)


    return 0
}
