
extern("c") fn puts(format_string u8*) i32
extern("c") fn printf(format_string u8*, args opaque...) i32
extern("c") fn fopen(file_path u8*, flags u8*) opaque*

// TODO: remove from std
type Token struct {
    num i32
}

